# GoodMemoryBot — MVP Spec v0.1

## 1. Цель MVP
Сделать персонального Telegram-бота, который позволяет быстро превратить сообщение в чате в событие (встречу) или напоминание в календаре пользователя.

MVP успешен, если пользователь:
- подключает календарь один раз,
- пересылает сообщения боту,
- создаёт 1–2 события/напоминания в день без лишних действий.

---

## 2. Проблема
Важные договорённости и “не забыть сделать” часто остаются в мессенджерах.
Пользователь:
- переводит сообщение в статус “новое” (непрочитанное),
- откладывает,
- сообщение уезжает в историю,
- часть вещей забывается.

Ручное добавление в календарь занимает время и требует “выковыривания” деталей из сообщения.

---

## 3. Решение (MVP)
Telegram-бот принимает пересланное сообщение и помогает в несколько нажатий создать:
- 📅 встречу в календаре
- ⏰ напоминание в календаре

Перед созданием:
- проверяет занятость выбранного слота
- показывает превью для подтверждения

---

## 4. Пользовательский сценарий
1. Пользователь получает сообщение в Telegram, содержащее задачу или договорённость.
2. Пользователь пересылает сообщение боту.
3. Бот предлагает:
   - 📅 Создать встречу
   - ⏰ Создать напоминание
4. Пользователь выбирает дату/время (через кнопки “сегодня/завтра/выбрать” и/или ручной ввод).
5. Бот проверяет занятость выбранного времени (free/busy).
6. Бот показывает превью события.
7. Пользователь подтверждает.
8. Событие создаётся в календаре пользователя.

---

## 5. Входит в MVP
- Telegram Bot
- Google OAuth для Google Calendar (подключение один раз)
- Один календарь по умолчанию
- Создание события (встреча)
- Создание напоминания (как событие в календаре)
- Проверка занятости выбранного времени
- Превью перед созданием

---

## 6. Не входит в MVP (жёстко)
- AI-анализ/понимание текста сообщений
- автоматический парсинг времени из текста
- контакты
- задачи/списки задач
- CRM
- приглашения участников / отправка инвайтов
- выбор нескольких календарей
- Outlook/iCloud
- работа “внутри групп” как отдельный режим (бот в группах)

---

## 7. Критерий готовности MVP
MVP готов, когда пользователь может:
- пройти OAuth и подключить календарь,
- переслать сообщение,
- создать напоминание/встречу с проверкой занятости,
- увидеть событие в своём Google Calendar.

---

## 8. Технологический стек (для MVP)
- TypeScript + Node.js
- Telegram Bot API
- Google Calendar API (OAuth2 + refresh token)
- Хранение токенов: минимально необходимое (например, SQLite/JSON/простая БД) — выбор уточняется в реализации.